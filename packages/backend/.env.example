# Server Configuration
PORT=3000
NODE_ENV=development

# Logging
LOG_LEVEL=debug

# The SQLite path is intentionally fixed by prisma/schema.prisma. Runtime
# credentials and ticket identity normally come from the private root .env.
