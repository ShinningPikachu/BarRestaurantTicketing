{
  "name": "bar-restaurant-ticketing",
  "private": true,
  "engines": {
    "node": ">=20.19.4 <23",
    "npm": ">=10"
  },
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "xcode": {
      "uuid": "^11.1.1"
    }
  },
  "scripts": {
    "bootstrap": "npm ci && npm run -w backend prisma:generate",
    "build": "npm run -w backend build && npm run -w frontend build && npm run -w shared build",
    "dev": "node scripts/dev-lan.mjs",
    "install:desktop": "bash Install_BarRestaurantTicketing.sh",
    "open": "node scripts/open-web.mjs",
    "stop": "node scripts/stop-local.mjs",
    "package:linux": "node scripts/build-linux-run.mjs",
    "package:linux:offline": "node scripts/build-linux-run.mjs --include-node-modules",
    "android:apk": "bash scripts/build-android-apk.sh",
    "test": "npm run -w backend test",
    "check:fast": "npm run -w backend test && npm run -w shared build && npm run -w frontend typecheck",
    "check": "npm run -w backend test && npm run -w backend build && npm run -w shared build && npm run -w frontend build"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "concurrently": "^8.0.0",
    "typescript": "~5.8.3"
  }
}
